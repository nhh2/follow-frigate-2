<!DOCTYPE html>
<html>
  <head>
  </head>
  
  <body>

  <div class="container">
        <h3> <%= @the_post.title %> </h3>
        <p> <%= simple_format @the_post.content %> </p>
        <p> Written on <%= @the_post.created_at.strftime("%B %e, %Y") %> </p>
      </div>
    </div>
  </div>

  </body>
</html>
      